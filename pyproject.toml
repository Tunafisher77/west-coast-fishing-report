[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "west-coast-fishing-report"
version = "0.2.0"
description = "Daily West Coast fishing intelligence report"
requires-python = ">=3.11"
dependencies = ["ephem>=4.1.5"]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[project.scripts]
wcfr = "wcfr.main:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
